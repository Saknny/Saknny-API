export enum ENUM_FILE_IMAGE_MIME {
  JPG = 'image/jpg',
  JPEG = 'image/jpeg',
  PNG = 'image/png',
  APNG = 'image/apng',
  AVIF = 'image/avif',
  GIF = 'image/gif',
  WEBP = 'image/webp',
  SVG = 'image/svg+xml',
}

export enum ENUM_FILE_DOC_MIME {
  PDF = 'application/pdf',
}

export enum ENUM_FILE_TYPE {
  IMAGE = 'IMAGE',
  PDF = 'PDF',
  VIDEO = 'VIDEO',
}
